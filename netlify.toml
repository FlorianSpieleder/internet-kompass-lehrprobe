[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  command = "vite --host 0.0.0.0"
  targetPort = 5173
  port = 8888
  publish = "dist"
  framework = "#custom"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
